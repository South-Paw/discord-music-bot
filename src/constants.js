const LOG_INFO = 'info';
const LOG_WARN = 'warn';
const LOG_ERROR = 'error';
const LOG_DEBUG = 'debug';

const SEND = 'send';
const REPLY = 'reply';
const DIRECT_MESSAGE = 'direct_message';

module.exports = {
  LOG_INFO,
  LOG_WARN,
  LOG_ERROR,
  LOG_DEBUG,
  SEND,
  REPLY,
  DIRECT_MESSAGE,
};

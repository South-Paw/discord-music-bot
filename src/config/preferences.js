const COMMAND_PREFIX = 'command_prefix';

const preferenceConstants = {
  COMMAND_PREFIX,
};

const defaultPreferences = {
  [COMMAND_PREFIX]: '!',
};

module.exports = {
  preferenceConstants,
  defaultPreferences,
};

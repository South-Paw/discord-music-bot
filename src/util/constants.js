const defaults = {
  aliasesFilePath: 'aliases.json',
  autoPlaylistFilePath: 'autoplaylist.txt',
  commandIdentifer: '!',
};

const messages = {
  mentionResponse: 'Use `!help` to see the command list.',
};

module.exports = {
  defaults,
  messages,
};
